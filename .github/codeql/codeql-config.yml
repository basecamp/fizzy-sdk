queries:
  - uses: security-and-quality

paths:
  - .github/
  - go/
  - typescript/
  - ruby/
  - swift/
  - kotlin/
  - rust/

paths-ignore:
  # Generated SDK code (fix in the generator or spec, not here).
  # Note: paths-ignore only prevents source-level extraction (JS, Ruby).
  # For compiled languages (Go, Swift, Kotlin), generated code enters the
  # database via build tracing — the workflow's SARIF filter step strips
  # those alerts before upload.
  - go/pkg/generated/
  - typescript/src/generated/
  - typescript/dist/
  - ruby/lib/fizzy/generated/
  - swift/Sources/Fizzy/Generated/
  - kotlin/sdk/src/commonMain/kotlin/com/basecamp/fizzy/generated/
  - rust/fizzy-sdk/src/generated/
  # The actions analysis extracts lockfiles as YAML and reports syntax errors
  # in them; no language analyses lockfiles.
  - '**/Gemfile.lock'
  # Test infrastructure
  - conformance/
  - kotlin/conformance/
  - rust/fizzy-sdk/tests/

query-filters:
  - exclude:
      kind: [diagnostic, metric]
