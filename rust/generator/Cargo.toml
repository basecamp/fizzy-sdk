[package]
name = "fizzy-sdk-generator"
version = "0.0.0"
description = "Generates the Rust Fizzy SDK's types, services and route table from openapi.json"
publish = false
edition.workspace = true
authors.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[[bin]]
name = "fizzy-sdk-generator"
path = "src/main.rs"

[dependencies]
heck = "0.5"
serde = { workspace = true }
serde_json = { workspace = true, features = ["preserve_order"] }
toml = "0.9"
