// Generated by fizzy-sdk-generator from openapi.json and behavior-model.json. DO NOT EDIT.

use crate::http::Method;
use crate::route::{Pagination, ParamKind, ParamRole, Retry, Route, RouteParam};

/// `CloseWidget` — `POST /{accountId}/widgets/{widgetNumber}/closure.json`.
///
/// Sent up to 3 times, backing off from 1000 ms, when the answer is 429, 500 or 503. Idempotent, so a resend is safe.
pub static CLOSE_WIDGET: Route = Route {
    id: "CloseWidget",
    service: "Widgets",
    method: Method::POST,
    path: "/{accountId}/widgets/{widgetNumber}/closure.json",
    pattern: "/{accountId}/widgets/{widgetNumber}/closure",
    account_scoped: true,
    resource_type: "widget",
    params: &[RouteParam {
        name: "widgetNumber",
        role: ParamRole::Parent,
        kind: ParamKind::Int32,
    }],
    idempotent: true,
    readonly: false,
    pagination: Pagination::None,
    retry: Some(Retry {
        max: 3,
        base_delay_ms: 1000,
        retry_on: &[429, 500, 503],
    }),
};

/// `CreateSession` — `POST /session.json`.
///
/// Sent once and never resent.
pub static CREATE_SESSION: Route = Route {
    id: "CreateSession",
    service: "Sessions",
    method: Method::POST,
    path: "/session.json",
    pattern: "/session",
    account_scoped: false,
    resource_type: "session",
    params: &[],
    idempotent: false,
    readonly: false,
    pagination: Pagination::None,
    retry: None,
};

/// `DestroySession` — `DELETE /session.json`.
///
/// Sent once and never resent.
pub static DESTROY_SESSION: Route = Route {
    id: "DestroySession",
    service: "Sessions",
    method: Method::DELETE,
    path: "/session.json",
    pattern: "/session",
    account_scoped: false,
    resource_type: "session",
    params: &[],
    idempotent: true,
    readonly: false,
    pagination: Pagination::None,
    retry: None,
};

/// `GetWidget` — `GET /{accountId}/widgets/{widgetNumber}`.
///
/// Sent up to 3 times, backing off from 1000 ms, when the answer is 429, 500 or 503.
pub static GET_WIDGET: Route = Route {
    id: "GetWidget",
    service: "Widgets",
    method: Method::GET,
    path: "/{accountId}/widgets/{widgetNumber}",
    pattern: "/{accountId}/widgets/{widgetNumber}",
    account_scoped: true,
    resource_type: "widget",
    params: &[RouteParam {
        name: "widgetNumber",
        role: ParamRole::Recording,
        kind: ParamKind::Int32,
    }],
    idempotent: true,
    readonly: true,
    pagination: Pagination::None,
    retry: Some(Retry {
        max: 3,
        base_delay_ms: 1000,
        retry_on: &[429, 500, 503],
    }),
};

/// `ListWidgets` — `GET /{accountId}/widgets.json`.
///
/// Sent up to 3 times, backing off from 1000 ms, when the answer is 429, 500 or 503.
///
/// Paginated: the answer is a page with `page` as its cursor, and the next one is read with `next_page`.
pub static LIST_WIDGETS: Route = Route {
    id: "ListWidgets",
    service: "Widgets",
    method: Method::GET,
    path: "/{accountId}/widgets.json",
    pattern: "/{accountId}/widgets",
    account_scoped: true,
    resource_type: "widget",
    params: &[],
    idempotent: true,
    readonly: true,
    pagination: Pagination::Link { page_param: "page" },
    retry: Some(Retry {
        max: 3,
        base_delay_ms: 1000,
        retry_on: &[429, 500, 503],
    }),
};

/// Every route the SDK knows, one per operation, by operation id.
pub static ROUTES: &[&Route] = &[
    &CLOSE_WIDGET,
    &CREATE_SESSION,
    &DESTROY_SESSION,
    &GET_WIDGET,
    &LIST_WIDGETS,
];
